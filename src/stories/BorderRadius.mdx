import { Meta, Canvas } from '@storybook/addon-docs/blocks'

import * as BorderRadiusStories from './BorderRadius.stories'

<Meta of={BorderRadiusStories} />

# Border Radius

## Usage

We use **4 types** of rounded corners for elements.

You can use them as classes generated by Tailwind CSS.

_Each story is named in the sidebar after the relevant class name._

To reset border-radius to 0, use `rounded-none`.

<Canvas of={BorderRadiusStories.RoundedSm} />
<Canvas of={BorderRadiusStories.Rounded} />
<Canvas of={BorderRadiusStories.RoundedLg} />
<Canvas of={BorderRadiusStories.RoundedFull} />
